use serde::{Deserialize, Serialize};

/// Result returned from AESM client's `get_quote`
#[derive(Debug, Serialize, Deserialize)]
pub struct QuoteResult {
    /// Enclave quote payload
    pub quote: Vec<u8>,
    /// Report generated by QE
    pub qe_report: Vec<u8>,
}
